@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="~/css/admin-user-style/admin.css" asp-append-version="true">
    @RenderSection("Styles", required: false)
</head>
<body>
    <div class="sidebar">
        <div class="sidebar-toggle">
            <span></span>
            <span></span>
            <span></span>
        </div>
        <nav class="menu">
            <ul>
                <li><a asp-controller="Admin" asp-action="Index">Home</a></li>
                <li><a asp-controller="Admin" asp-action="Profile">Profile</a></li>
                <li><a asp-controller="Admin" asp-action="PasswordListing">Passwords</a></li>
                <li><a asp-controller="Admin" asp-action="AddUpdatePassword">Add/Edit Password</a></li>
                <li><a asp-controller="Admin" asp-action="UserListing">Users</a></li>
                <li><a asp-controller="Admin" asp-action="AddUpdateUser">Add/Edit User</a></li>
                <li><a asp-controller="Admin" asp-action="RoleListing">Roles</a></li>
                <li><a asp-controller="Admin" asp-action="AddUpdateRole">Add/Edit Role</a></li>
                <li><a asp-controller="Admin" asp-action="CompanyLogs">Log</a></li>
                <li><a asp-controller="Login" asp-action="Logout" class="btn btn-red-exit">Exit</a></li>
            </ul>
        </nav>
    </div>
    <div class="content">
        @RenderBody()
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const sidebarToggle = document.querySelector('.sidebar-toggle');
            const sidebar = document.querySelector('.sidebar');

            sidebarToggle.addEventListener('click', function () {
                sidebar.classList.toggle('closed');
                if (sidebar.classList.contains('closed')) {
                    sidebar.style.width = '60px';
                } else {
                    sidebar.style.width = '250px';
                }
            });
        });
    </script>
    @RenderSection("Scripts", required: false)
</body>
</html>

